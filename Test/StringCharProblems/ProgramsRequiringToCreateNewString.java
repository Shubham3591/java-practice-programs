package StringCharProblems;

public class ProgramsRequiringToCreateNewString {

	public static void main(String[] args) {

		/*String s = "ITs 1 SimPle AnD 2 easY";

		
		
		
		
		// Create a New String containing only upper case letters

		String s = "ITs 1 SimPle AnD 2 easY";
		String sn = "";
		for (int i = 0; i < s.length(); i++) {
			char c = s.charAt(i);
			if (Character.isUpperCase(c)) {
				sn = sn + c;
			}
		}
		System.out.print(sn);

		
		
		
		
		// Create a New String with Digit first and then Characters

		String s1 = "01ItS2 Very134 Simple9755";
		String d = "";
		String ns = "";

		for (int i = 0; i < s1.length(); i++) {
			char c = s1.charAt(i);
			if (Character.isDigit(c))
				d = d + c;
			else
				ns = ns + c;
		}
		System.out.print(ns + " __ " + d);

		
		
		
		
		// Create a new string having first Uppercase letters and then Lower case

		String u = "";
		String l = "";

		for (int i = 0; i < s.length(); i++) {
			char c = s.charAt(i);
			if (Character.isUpperCase(c))
				u = u + c;
			else
				l = l + c;
		}
		System.out.print(u + " " + l);

		
		
		
		// Create a new string having all white Spaces removed

		String s1 = s.replaceAll("\\s", "");
		System.out.print(s1);

		String ns = "";
		for (int i = 0; i < s.length(); i++) {
			char c = s.charAt(i);

			if (!Character.isWhitespace(c))
				ns = ns + c;
		}
		System.out.print(ns);

		
		
		// Write a program to change case of entire String

		String ns = "";
		for (int i = 0; i < s.length(); i++) {
			char c = s.charAt(i);
			if (Character.isUpperCase(c))
				ns = ns + Character.toLowerCase(c);
			else
				ns = ns + Character.toUpperCase(c);
		}
		System.out.print(ns);

		
		
		// Write a program which moves each letter by 2
		String ns = "";
		String s1 = "Its Simply eazy";
		for (int i = 0; i < s1.length(); i++) {
			char c = s1.charAt(i);
			if (c == 'Y' || c == 'Z' || c == 'y' || c == 'z')
				c -= 26;
			c += 2;
			ns = ns + c;
		}
		System.out.print(ns);   */

	}
}
